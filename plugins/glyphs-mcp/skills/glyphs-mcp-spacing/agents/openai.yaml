interface:
  display_name: "Glyphs MCP Spacing"
  short_description: "Review and apply v2 grid-aware spacing safely."
  default_prompt: "Use $glyphs-mcp-spacing to review exact-layer width and optional foreground-translation proposals, verify grid snapping and ownership guards, and apply only an authorized atomic batch."

policy:
  allow_implicit_invocation: true
