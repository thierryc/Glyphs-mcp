# RV02 execution log

Machine timestamps for public calls are in calls.jsonl. Native action timestamps,
timings and results are in tasks/*/facts-first.json, export.json and snippet
revision results. Native CLI/Xcode logs retain their own timestamps. This log
describes workflow decisions; it is not a claim that every assistant/tool event
was instrumented.

1. Froze source, installed skills/ownership, official corpus, protected font
   files, runtime identities and host load.
2. Read the accepted RV02 plan, current skills, installer ownership rules and
   RV01 evidence. Added the focused OpenType route, precision/API references and
   matching Starter/package routing.
3. Executed the documented snippets in unit doubles and ran relevant Python and
   Swift suites. Corrected two package-inventory assertions and the new Swift
   fixture's expectation about a generic native-skill conflict message.
4. Built the candidate with the existing payload builder; validated its skill
   metadata, links, mirror trees and isolated offline retrieval. The official
   corpus and bridge/sidecar fingerprints matched the baseline.
5. Ran the native task batch, deliberate rounding-flag exception, numerical
   curve/interpolation proofs, collateral checks and actual export/shaping.
   Created, validated, ran and revised a real scaffolded audit file.
6. Refreshed the old RV01 Scripts entry. The GUI Go To sheet initially lost path
   text; setValue/Return selected an old suggestion, paste timed out, and a
   shorter typed path resolved correctly after checking the field before Return.
   Kept Position for the twenty recorded native import issues.
7. Installed the tested skills once via the existing installer; backed up and
   replaced only the reviewed OpenType conflict. Verified all eleven identities.
   No Glyphs restart or runtime install was invoked.
8. Ran public metadata/missing-glyph/same-ID checks, then closed the disposable
   copy and verified stale-ID rejection. Executed installed precision/API notes
   and independently read the sampled metadata.
9. Restored the protected window and verified that RV01's script entry was absent.
   A final screenshot request was unavailable; accessibility evidence is retained.
   No screenshot is counted as a pass.
10. Audited preservation, token/latency limits and commit scope; published the
    separate RV02 report and coverage entry without rewriting RV01.
