interface:
  display_name: "Glyphs MCP Kerning"
  short_description: "Review and apply kerning bumper changes safely."
  default_prompt: "Use $glyphs-mcp-kerning to review kerning bumper suggestions, do a dry run first, and only apply approved changes."

policy:
  allow_implicit_invocation: true
