# P13 curvature display — predefined protocol

Compare installed private v2 Beta 1 / Glyphs 4.1 (4107) first, unchanged v1 1.11.0 / Glyphs 3.5 (3532) second. Freeze runtime, companion, skill, receipt and protected font files. No source fix or installation during measurement. Normal periodic autosaving remains enabled; stop on a new crash without relaunching.

Use reproducible `CurvatureTest.glyphs`, three fixed masters, fractional coordinates, metadata, 3-cubic small and 40-cubic dense cases, 129-cubic bound, straight, empty, mixed path/component and component-only controls. Fresh disposable copy for first attempt, one warm-up and five timed repetitions per size per version. Confirm native geometry equivalence independently before comparing results. No user font writes/saves.

V2 activation uses View → Show Curve Inspector (verify exact native label); v1 uses public `set_curve_review_overlay`, `overlays=["curvature"]` for equivalent scope. No helper substitutes for activation, refresh, editing or Undo. Setup/oracle may open disposable fonts, choose explicit layers/nodes and read actual cache/drawing evidence. Actual node nudge and Undo/Redo use the native UI. Seven-tool catalog remains unchanged. No jobs/cancellation/discard apply to display-only activation; reversal is disabling the reporter and native Undo for the test edit.

Each complete trial: enable on a fresh copy; verify comb matches current layer; nudge selected off-curve node one native unit, verify refreshed geometry and dirty state; native Undo, verify exact restoration; native Redo and final Undo; disable/re-enable and verify; disable then close only the disposable. Separate controls cover three-master switching, empty/straight/component cases, Font View, text/pan/zoom visibility, and >128 cubics. Capture representative native images. Never count a populated cache alone as proof of visible pixels. Unavailable visual evidence or repetitions remain explicit.

Independent oracle uses native GSNode values and native object identities; exact preservation for all unchanged data, widths, metadata, anchors, components and restoration. Curvature/comb numerical checks use independent Bernstein derivatives and tolerance 1e-9. Inspect completeness claims separately from bounded work. Do not interpret curvature as an aesthetic grade.

Record time to initial useful evidence, refresh, history restoration and total complete workflow, actual UI actions, MCP calls, retries and native oracle overhead separately. UI-tool wall times include automation and screenshot waits; they are not isolated native latency. Record native callbacks and main-queue lag only with temporary bounded instrumentation, including two seconds after mutation. Budgets p95<50 ms, max<200 ms apply only to the native probe. First/warm-up separate from five samples; median, range, nearest-rank p95/max, host load and fixed order disclosed.

Token estimates use tiktoken 0.12.0/o200k_base and compact sorted literal-Unicode requests plus parsed responses; skill payload separately. Actual billed tokens unavailable unless provided. UI/oracle context must not be presented as zero cost. Score discovery, instructions, evidence, recovery and effort 1–5 with observed examples. Preserve original reports, log v1 findings without fixing them, propose only evidenced lean v2 changes. Restore reporter settings, temporary helper and test fonts; verify identities, files and crash absence after cleanup.
