interface:
  display_name: "glyphs"
  short_description: "Identify the Glyphs connection and route font work"
  default_prompt: "Use $glyphs to identify this MCP connection and choose its matching font workflow."

policy:
  allow_implicit_invocation: false
