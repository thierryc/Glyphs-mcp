interface:
  display_name: "glyphs-mcp-outlines-docs"
  short_description: "Inspect native layer evidence and supported outlines"
  default_prompt: "Use $glyphs-mcp-outlines-docs for this outline task. Reuse verified connection and document context; load only the relevant focused reference."

policy:
  allow_implicit_invocation: true
