# Lean grouping candidate

Implemented and qualified offline; installation/UI checks are pending the open font’s unsaved edits. Only existing native grouping hooks change. Native Redo behavior is accepted as-is.

The archive contains all source-gate evidence, initial failures, successful tests, fixtures, prepared GUI helper, build identities, frozen file hashes and coverage. Every member is checksum verified. Extract into a fresh directory and open reports/v1-v2/dirty-grouping-20260914/report.md for working evidence links. Use new output paths before replay; do not overwrite historical logs. No private font source, credentials or release publication.
