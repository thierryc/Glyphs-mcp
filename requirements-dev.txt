-r requirements.txt

# Repository tests and deterministic source benchmarks. These are intentionally
# separate from the dependencies installed into Glyphs for the MCP runtime.
glyphsLib==6.10.1
fontmake==3.10.1
Pillow==12.0.0
pytest==8.4.2
