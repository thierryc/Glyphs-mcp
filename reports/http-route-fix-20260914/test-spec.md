# Configured HTTP route correction

Change only the existing HTTP path argument to `/mcp/`; preserve all seven tools,
stdio/stateless HTTP, current port, management security and native/job behavior.
Use the pinned installed FastMCP 2.12.0 implementation. Do not edit skills or v1.

1. Capture a live baseline using redirects disabled for explicit path probes and
   enabled for the currently redirected configured-path tool calls.
2. Compare exact MCP handshakes, catalog, seven fixed service outcomes and errors
   through actual ASGI transports. Test the alternate route and control security.
3. Run relevant/full lean regressions; build twice and require deterministic full
   payload identity, with only the sidecar changed from P13.
4. Use the existing installer. Its app-closed guard is mandatory; restore only
   the verified clean disposable font after a needed native relaunch.
5. Repeat the read-only installed probe with redirects disabled, first/warm-up/five
   reads. Use public discovery and fresh IDs after restart. Compare responses,
   allowing only expected sidecar identity and native document-lifetime differences.
6. Verify the normal SDK client and configured connector, installed hashes,
   preserved fonts/skills/v1/configuration and absence of new crash reports.

This focused route test uses the existing disposable Font View control, not a new
full P07 font/edit benchmark. Native timings are unavailable. Keep outliers and
host-load/relaunch limitations explicit; do not infer a large-stall fix.
