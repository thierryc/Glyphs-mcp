interface:
  display_name: "Glyphs MCP Scripting"
  short_description: "Run staged or open-world Glyphs Python with rollback."
  default_prompt: "Use $glyphs-mcp-scripting when typed tools do not fit, defaulting document edits to staged execution."
policy:
  allow_implicit_invocation: true
