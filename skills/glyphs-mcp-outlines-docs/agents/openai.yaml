interface:
  display_name: "glyphs-mcp-outlines-docs"
  short_description: "Inspect native layer evidence and supported outlines"
  default_prompt: "Use $glyphs to identify this connection, then follow its matching outline inspection workflow."

policy:
  allow_implicit_invocation: true
