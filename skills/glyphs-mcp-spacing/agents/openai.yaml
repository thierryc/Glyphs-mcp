interface:
  display_name: "glyphs-mcp-spacing"
  short_description: "Measure native spacing and review supported changes"
  default_prompt: "Use $glyphs to identify this connection, then follow its matching spacing workflow."

policy:
  allow_implicit_invocation: true
