# Consolidated dev requirements. For runtime (vendored inside the plug‑in), see
# requirements.runtime.txt. For full local dev setup, use:
 -r requirements.dev.txt

# Glyphs runtime ships these modules; captured here for clarity only.
# They are not installed via pip because Glyphs bundles PyObjC and the GlyphsApp API.
# Modules: GlyphsApp, objc, AppKit
