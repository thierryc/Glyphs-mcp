Repeat Report 01 without changing installed code or configured skills. Rebuild the candidate from the release checkout; compare component fingerprints to prior qualification. Rerun the same Python and Swift regression suites. Run isolated installed-source baseline then candidate, first attempt, warm-up and five verified connection repetitions, plus 20 status probes. Record dispatch, callback and sidecar assembly separately with the existing temporary instrumentation. Repeat installed endpoint measurements and exercise the actual connection-specific candidate entry control. Snapshot code, receipts, applications and skills before/after. Pass the advancement gate when identity/routing and regressions are sound and the unchanged baseline can be targeted explicitly; do not require deployment to proceed with read-only comparison. Record unavailable or externally changed evidence separately. No font edits or saves in this test.
