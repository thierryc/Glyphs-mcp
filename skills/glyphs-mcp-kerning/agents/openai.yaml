interface:
  display_name: "glyphs-mcp-kerning"
  short_description: "Inspect stored kerning and review collision repairs"
  default_prompt: "Use $glyphs and $glyphs-mcp-kerning to inspect stored kerning. Prepare a collision repair only when requested."
