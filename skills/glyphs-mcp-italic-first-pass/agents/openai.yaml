interface:
  display_name: "Glyphs MCP Italic First Pass"
  short_description: "Create guarded first-pass italic or oblique glyphs."
  default_prompt: "Use $glyphs-mcp-italic-first-pass to stage a bounded Roman-to-italic construction draft, present its semantic diff, and confirm only the exact approved execution."

policy:
  allow_implicit_invocation: true
