interface:
  display_name: "Glyphs MCP Kerning"
  short_description: "Review pair and contextual kerning safely."
  default_prompt: "Use $glyphs-mcp-kerning to finish spacing and ordinary pairs before exact-sequence contextual kerning, account for coverage, disclose additive behavior and unmeasured geometry, and apply only explicit approved values."

policy:
  allow_implicit_invocation: true
