# V2 actions justified by P13 — proposed, not implemented

1. **Focused curvature guidance — first priority, small effort.** Add `glyphs-mcp-outlines-docs/references/curvature-display.md`; link it from the short skill. Explain View → Show Curve Inspector, Select/text-mode behavior, native Zoom to Active Layer when needed, teal/pink signed curvature, raw paths only, component omission, at most 128 cubics, reduced uniform sampling and the 0.12em clamp. Explain how to enable, inspect after edits/layer switches/Undo and disable without a job or Save. Reuse known connection/document context. Align the focused invocation wording and packaged copies through existing skill checks; preserve invocation policy. Native evidence: all shared geometry passes, while menu/coverage details required source and oracle inspection.

2. **Compact companion coverage notice — second priority, bounded implementation.** Extend only the existing visible-layer extraction/result metadata and refresh key/cache. Detect an exceeded cubic bound with bounded look-ahead and report partial coverage explicitly; do not fabricate an exact total. Record component omission and sampling/clamp limits needed to interpret the display. Draw one unobtrusive status notice from prepared metadata, with no traversal or mutation in drawing callbacks. Preserve existing geometry, worker behavior, 128-cubic/2,000-stroke bounds and seven MCP tools. Avoid adding a public overlay control solely to chase the measured UI-action difference.

   Qualify on fresh P13 copies, adding a closed-contour wraparound control to the existing open curves: <=128 and >128 cubics, empty/straight/component-only/mixed layers, node edits, three masters, disable/re-enable, native Undo/Redo, no stale notice after switching layers, exact preservation and native timing. Keep current measurements intact. Build/install only a separately identified candidate when implementation is authorized.

3. **Keep separate: queue tail and Undo dirty state.** The corrected native queue probe captures a 382.49ms v2 maximum despite short drawing/extraction callbacks. Its isolated cause and probe overhead are unverified; compare matched UI/host load before proposing an optimization. The primary trials restore exact font data but leave dirty=true in both hosts. Keep that deferred investigation separate. No autosave change, cache/watch service, custom Undo, release publication or v1 fix is justified here.

V1's stronger public state is a useful evidence-design reference, not a requirement to reproduce its larger tool catalog. The recommended first iteration is skill guidance plus honest companion coverage feedback.
