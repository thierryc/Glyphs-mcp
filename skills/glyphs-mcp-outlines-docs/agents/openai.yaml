interface:
  display_name: "Glyphs MCP Outlines"
  short_description: "Edit outlines safely and look up bundled docs."
  default_prompt: "Use $glyphs-mcp-outlines-docs to inspect selected nodes, prefer dedicated outline tools, and fetch only the docs pages needed."

policy:
  allow_implicit_invocation: true
