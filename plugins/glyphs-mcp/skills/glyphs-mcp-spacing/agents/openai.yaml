interface:
  display_name: "Glyphs MCP Spacing"
  short_description: "Review and apply v2 width-only spacing safely."
  default_prompt: "Use $glyphs-mcp-spacing to review v2 width-only proposals, separate geometry translation from width changes, and apply only authorized eligible widths."

policy:
  allow_implicit_invocation: true
